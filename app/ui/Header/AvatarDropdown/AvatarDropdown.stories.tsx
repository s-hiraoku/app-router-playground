import { Meta, StoryObj } from "@storybook/react";
import { AvatarDropdown } from "./AvatarDropdown";

export default {
  component: AvatarDropdown,
} as Meta<typeof AvatarDropdown>;

export const Default: StoryObj<typeof AvatarDropdown> = {
  args: {},
};
